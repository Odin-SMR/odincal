[tox]
envlist = py27

[testenv]
deps =
    -r../requirements.txt
    mockito
    pytest
    pytest-cov
commands =
    pytest \
        --cov oops --cov-branch --cov-report xml --cov-report term \
        --junitxml result.xml \
        {posargs}
